version: 2

models:
  - name: gold_monthly_booking_counts_per_neighborhood
    config:
      tags: ['gold_models','null_test']
    columns:
      - name: total_booking
        tests:
          - not_null:
              config:
                severity: warn